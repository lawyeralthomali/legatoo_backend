name: Auto-Deploy FastAPI Backend to Hostinger

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Create production deployment files
      run: |
        mkdir -p deploy_files
        
        # Create production startup script
        cat > deploy_files/start_production.py << 'EOF'
        #!/usr/bin/env python3
        """
        Production startup script for Legatoo FastAPI Backend
        """
        import os
        import sys
        import uvicorn
        from pathlib import Path
        
        # Add the project root to Python path
        project_root = Path(__file__).parent
        sys.path.insert(0, str(project_root))
        
        # Set production environment variables
        os.environ.setdefault('ENVIRONMENT', 'production')
        os.environ.setdefault('LOG_LEVEL', 'INFO')
        
        if __name__ == "__main__":
            print("🚀 Starting Legatoo FastAPI Backend - Production Mode")
            print("📋 Deploying actual FastAPI backend with all endpoints")
            print("🔄 Deployment triggered by: GitHub Actions Push")
            print("🌐 API Documentation: https://legatoo.westlinktowing.com/api/docs")
            print("📊 Swagger UI: https://legatoo.westlinktowing.com/api/swagger.html")
            
            # Start the FastAPI application
            uvicorn.run(
                "app.main:app",
                host="0.0.0.0",
                port=8000,
                reload=False,
                workers=1,
                log_level="info"
            )
        EOF
        
        # Create production deployment script
        cat > deploy_files/deploy_production.sh << 'EOF'
        #!/bin/bash
        echo "🚀 Starting Production FastAPI Backend Deployment..."
        echo "📅 Deployment Time: $(date)"
        echo "🔄 Triggered by: Push to Repository"
        
        cd /home/$USER/legatoo_backend
        
        # Stop old processes
        echo "🛑 Stopping old backend processes..."
        pkill -f "start_production.py" || true
        pkill -f "exact_routes_backend.py" || true
        pkill -f "deploy_backend.py" || true
        pkill -f "run_fastapi.py" || true
        pkill -f "uvicorn" || true
        
        # Install/update Python dependencies
        echo "📦 Installing Python dependencies..."
        pip3 install --upgrade pip
        pip3 install -r requirements.txt
        
        # Create database if it doesn't exist
        echo "🗄️ Initializing database..."
        python3 -c "
        import asyncio
        from app.db.database import create_tables
        asyncio.run(create_tables())
        print('Database tables created successfully!')
        "
        
        # Start production backend
        echo "▶️ Starting production FastAPI backend..."
        nohup python3 start_production.py > production.log 2>&1 &
        
        # Wait for startup
        sleep 5
        
        # Test backend health
        echo "🧪 Testing backend health..."
        if curl -s http://localhost:8000/health > /dev/null; then
            echo "✅ FastAPI Backend deployed successfully!"
            echo "🌐 API: https://legatoo.westlinktowing.com/api"
            echo "📚 Swagger UI: https://legatoo.westlinktowing.com/api/swagger.html"
            echo "📖 API Docs: https://legatoo.westlinktowing.com/api/docs"
            echo "🔐 Auth Endpoints: https://legatoo.westlinktowing.com/api/v1/auth/"
        else
            echo "❌ Backend health check failed"
            echo "📋 Checking logs..."
            tail -20 production.log
            exit 1
        fi
        EOF
        chmod +x deploy_files/deploy_production.sh

    - name: Upload files to Hostinger
      uses: appleboy/scp-action@v0.1.4
      with:
        host: ${{ secrets.HOSTINGER_HOST }}
        username: ${{ secrets.HOSTINGER_USERNAME }}
        password: ${{ secrets.HOSTINGER_PASSWORD }}
        port: ${{ secrets.HOSTINGER_PORT }}
        source: "deploy_files/*"
        target: "/home/${{ secrets.HOSTINGER_USERNAME }}/legatoo_backend/"
        strip_components: 1

    - name: Execute production deployment
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.HOSTINGER_HOST }}
        username: ${{ secrets.HOSTINGER_USERNAME }}
        password: ${{ secrets.HOSTINGER_PASSWORD }}
        port: ${{ secrets.HOSTINGER_PORT }}
        timeout: 60s
        command_timeout: 60s
        script: |
          cd /home/${{ secrets.HOSTINGER_USERNAME }}/legatoo_backend
          chmod +x deploy_production.sh
          ./deploy_production.sh